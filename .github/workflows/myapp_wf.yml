
name: myapp-workflow

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  job1:
    runs-on: ubuntu-latest

    steps:
    - name: shell commands
      run: |
        echo "Hello ${{ github.repository }}",
        
    - name: list files
      run: ls -la
